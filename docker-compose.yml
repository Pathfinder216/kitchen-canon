services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - data:/app/data
    environment:
      NODE_ENV: production
      DATABASE_URL: file:/app/data/database.db
      MEDIA_STORAGE_PATH: /app/data/media
    restart: unless-stopped

volumes:
  data:
